# Exemplo de variáveis de ambiente para desenvolvimento
# Copie este arquivo para `.env` e ajuste os valores conforme necessário.

# Endpoint da API de contato (opcional). Se não definido, o app usará https://apigcruz.geg.dev.br
# Pode incluir protocolo (https://) ou apenas host.
VITE_CONTACT_API_URL=https://apigcruz.geg.dev.br
